import React from "react";
// import Layout from "./layout";

const Home: React.FC<any> = () => (
  // <Layout>
  // here we are not importing layout component b'coz global layout automatically applies in all pages
  // </Layout>
  <div>
    <h1>Home Page</h1>
  </div>
);

export default Home;
